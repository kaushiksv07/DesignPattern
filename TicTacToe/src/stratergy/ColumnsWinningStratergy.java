package stratergy;

import model.Board;
import model.Move;
import model.enums.GameStatus;

public class ColumnsWinningStratergy implements WinningStratergy {

    @Override
    public GameStatus getGameStatus(Board board, Move move) {
        return null;
    }
}
