package stratergy;

import model.Board;
import model.Move;
import model.Player;

public class MediumBotStratergy implements BotStrategy{
    @Override
    public Move makeMove(Board board, Player player) {
        return null;
    }
}
