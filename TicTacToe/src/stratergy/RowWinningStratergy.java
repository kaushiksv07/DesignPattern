package stratergy;

import models.Board;
import models.Move;

public class RowWinningStratergy implements WinningStratergy {
    @Override
    public boolean checkWinningStratergy(Board board, Move move) {
        return false;
    }
}
